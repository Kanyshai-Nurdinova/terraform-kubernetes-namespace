# Usage
### Copy paste below code
```

module ns-demo {
  source = "Kanyshai-Nurdinova/namespace/kubernetes"
  name = "ns-demo"
  pod_quota = "12"
}
```

### Run
```
terraform init
terraform apply

```
